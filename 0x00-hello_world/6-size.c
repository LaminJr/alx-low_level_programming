#include <stdio.h>

/**
 * main - prints the sizes of the method
 * Return: always 0
 */

int main(void)
{
	printf("Size of a char: %lu byte(s)\n", sizeof(char);
	printf("Size of an int: %lu byte(0)\n", sizeof(int);
	return (0);
}
